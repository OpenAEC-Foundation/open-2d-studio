# Build output
target/

# Local verification harness (baseline md5s are captured per-machine
# from /c/Users/rickd/Desktop fixtures; not portable to other devs).
.verify.sh

# Stale per-session diagnostic dumps. The parser writes nothing to
# disk in production — these are scratch from interactive debugging
# sessions and can be regenerated by re-running dump examples in
# `examples/`. Tracked .txt under examples/ stays unaffected.
/dump_out.txt
/dxf_handles.txt
/types_trace.txt
/unk0_handles.txt
/unk_trace.txt
